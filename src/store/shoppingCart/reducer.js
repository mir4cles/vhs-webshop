const initialState = [
	{
		productId: 1,
		quantity: 1,
	},
];

export default function shoppingCartSliceReducer(state = initialState, action) {
	switch (action.type) {
		// case
		default: {
			return state;
		}
	}
}
